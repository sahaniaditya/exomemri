# Atlas backend - Phase 0 (thin capture endpoint).
# Normalized to UTF-8. Phase 1+ LLM/pipeline deps are deferred (see bottom).

# --- Core web/server ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
# Explicit: app/config.py points SSL_CERT_FILE at this bundle as a fallback
# for local installs missing a usable system CA store.
certifi>=2024.2.2

# --- Object store (Supabase Storage) + Auth ---
# Pulls in storage3, gotrue, postgrest, realtime.
supabase>=2.4
# EmailStr validation for auth payloads.
email-validator>=2.1

# --- Dev / test ---
ruff>=0.4
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# --- LLM (summary/chat) ---
# Claude Haiku via the official Anthropic SDK, see app/services/llm_service.py.
anthropic>=0.40

# --- Embeddings (chunk + query vectors for RAG chat) ---
# Anthropic has no embeddings endpoint; BAAI/bge-small-en-v1.5 via the
# Hugging Face serverless Inference API. See app/services/embedding_service.py.
huggingface_hub>=0.26
# feature_extraction returns numpy arrays.
numpy>=1.26

# --- RAG pipeline orchestration (chunk -> embed -> summarize) ---
langgraph>=0.2
langchain-core>=0.3

# --- Phase 1+ (deferred; not installed for the starter) ---
# langchain
# langgraph-checkpoint
# langsmith
