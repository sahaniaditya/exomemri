# Atlas backend - Phase 0 (thin capture endpoint).
# Normalized to UTF-8. Phase 1+ LLM/pipeline deps are deferred (see bottom).

# --- Core web/server ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Object store (Supabase Storage) + Auth ---
# Pulls in storage3, gotrue, postgrest, realtime.
supabase>=2.4
# EmailStr validation for auth payloads.
email-validator>=2.1

# --- Dev / test ---
ruff>=0.4
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# --- LLM (summary/chat) ---
# Used as an OpenAI-compatible client against Gemini's API gateway, see app/services/llm_service.py.
openai>=1.0

# --- Phase 1+ (deferred; not installed for the starter) ---
# langchain
# langchain-core
# langgraph
# langgraph-checkpoint
# langsmith
# anthropic
