"""Atlas backend — Phase 0 thin capture endpoint.

Phase 0 scope: accept a source capture and write its raw artifact to
Supabase Storage. No Postgres, no queue, no LLM. See docs/IMPLEMENTATION_PLAN.md.
"""
